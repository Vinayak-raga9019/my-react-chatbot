 # 🤖 My React Chatbot

A simple, clean AI Chatbot built with React and Vite. 
It features a "Brain" that responds to greetings, dates, and time queries.

## 🚀 How to Run
1. Clone the repository.
2. Run `npm install` to install the dependencies.
3. Run `npm run dev` to start the server.

## ✨ Features
* **Clean UI:** Minimalist design, focused on the conversation.
* **Auto-Reply:** The bot automatically replies to specific keywords.
* **Custom Avatars:** Uses custom user and robot images.

## 🛠️ Built With
* React.js
* Vite
* CSS3